Runtime V2.1A - Web / Localhost Review UI Prototype.

Create a local-only localhost review UI prototype using Python standard library. It must bind only to 127.0.0.1, render/export structured decisions, show safety state, and perform no hidden writes, provider calls, scheduler calls, training, action execution, or recall mutation.
