# Recovered Concepts

- `Risk assessment separates likelihood from impact` from `Likelihood refers to the probability of a risk` (qwen, risk_assessment, confidence 0.8 -> 0.86)
- `Counterfactual claims require comparison metrics` from `If the performance metrics of the two groups` (qwen, counterfactual_reasoning, confidence 0.8 -> 0.86)
- `Preventive maintenance transfer requires failure-rate evidence` from `The reusable concept is the principle of preventive` (llama, cross_domain_transfer, confidence 0.8 -> 0.86)
